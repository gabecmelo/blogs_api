const userValidations = require('./user.validations');
const categoryValidations = require('./category.validations');
const postValidations = require('./post.validations');

module.exports = {
  userValidations,
  categoryValidations,
  postValidations,
};
